import React from 'react';
// import styled from 'styled-components';

const Score = (props) => {
    return (
        <>
            <p>
                {props.scoreName}: 0
            </p>
        </>
    );
};

export default Score;